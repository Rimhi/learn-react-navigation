import {StackScreenProps} from "@react-navigation/stack";
import {RootStackParams} from "../navigator/StackNavigator";

export interface Props extends StackScreenProps<RootStackParams,'PersonaScreen'>{

}